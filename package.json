{
  "name": "vue-tailwind-design-system-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter ./packages/* run build",
    "dev": "pnpm --filter @acme/playground dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "echo 'Add ESLint later'"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vue": "^3.4.38",
    "@vitejs/plugin-vue": "^5.1.2"
  }
}